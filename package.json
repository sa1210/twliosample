{
  "name": "twilio-auth-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for SMS and Voice authentication using Twilio",
  "main": "index.js",
  "scripts": {
    "deploy": "gcloud functions deploy sendVerificationCode --runtime nodejs20 --trigger-http --allow-unauthenticated --env-vars-file .env.yaml && gcloud functions deploy verifyCode --runtime nodejs20 --trigger-http --allow-unauthenticated --env-vars-file .env.yaml"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.1.0",
    "firebase-admin": "^12.0.0",
    "twilio": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
